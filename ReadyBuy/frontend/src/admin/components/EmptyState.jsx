const EmptyState = ({
    title,
    subtitle,
}) => {

    return (

        <div className="text-center py-5">

            <h4>{title}</h4>

            <p>{subtitle}</p>

        </div>

    );

};

export default EmptyState;